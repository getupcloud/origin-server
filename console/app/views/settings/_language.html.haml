%h2= _("Idiom")

.btn-toolbar
  = semantic_form_for :lang,
                      :simple => true,
                      :method => :put,
                      :url => language_path do |f|
    = f.inputs :inline => true, :without_errors => true do
      .input-prepend
        %span.add-on Select console idiom
        = f.input :select, {collection: options_for_select({_('English') => 'en', _('Portuguese (Brazil)') => 'pt'}, session[:lang]), :include_blank => false, :input_html => {:style => 'width: auto', :title => _('Select language') }}

- content_for :javascripts do
  :javascript
    $('#lang_select').change(function (ev) {
      ev.target.form.submit();
    });
