- content_for :page_title, _("Delete Team")

%h1 Delete Team

= semantic_form_for @team, :html => {:method => :delete, :class => 'form'} do |f|
  %p 
    Are you sure you want to delete the team
    <strong>'#{@team.name}'?</strong>

  %p 
    This cannot be undone. Make sure this is something you really want to do!

  = f.semantic_errors
  = f.buttons do
    = link_to _('Cancel'), @referer || team_path(@team), :class => 'btn'
    = f.commit_button :label => _("Delete"), :button_html => {:class => 'btn btn-danger'}
    = f.loading
